{
  "name": "my-post-scraper",
  "version": "0.0.1",
  "description": "To scrape my facebook wall, public posts only.",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist/ && webpack --progress --colors ",
    "dev": "rm -rf dist/ && webpack --progress --colors && npm start",
    "start": "webpack-dev-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "soidid <soidid@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.9.0",
    "babel-loader": "^6.2.4",
    "babel-preset-es2015": "^6.9.0",
    "webpack": "^1.13.1",
    "webpack-dev-server": "^1.14.1"
  },
  "dependencies": {
    "superagent": "^2.3.0"
  }
}
